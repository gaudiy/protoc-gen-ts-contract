version: v2

modules:
  - path: proto

deps:
  # Pinned to match the @bufbuild/protovalidate 1.0.0 npm dependency.
  # buf dep update will fail while the vendored proto exists; refresh via
  # `buf export buf.build/bufbuild/protovalidate:v1.0.0 --output proto`.
  - buf.build/bufbuild/protovalidate:v1.0.0
